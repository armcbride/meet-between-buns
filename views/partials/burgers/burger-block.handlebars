<li>
	{{name}}

	<button class=" btn change-eaten" data-id="{{id}}" data-neweaten="{{eaten}}">
		{{#if eaten}}Eat!{{else}}Save for later{{/if}}
	</button>

	<button class="btn delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
